# CursorMochi Architecture v2

状态：建议实施基线；产品里程碑仍为 v0.1。编写日期：2026-09-14。

## 1. 范围与原则

保持 Rust stable + GTK4/gtk-rs + GIO/GLib。v0.1 交付：发现本地主题 → 查看真实静态/动画预览 → 明确应用 GNOME 配置 → 撤销本会话内上一次成功修改。

稳定与兼容不是框架名称自动提供的保证。所有支持声明来自实测记录。Rust 也不替代文件边界检查、资源限制、第三方依赖审查或进程隔离。

本版是四个小型 crate 组成的模块化单体。默认只有一个桌面进程，没有服务、动态插件、通用事件总线、数据库或第二套异步运行时。界面内的线程任务不等于常驻后台服务。

## 2. 四个 crate 与依赖方向

```text
crates/
├── cursormochi-core/       纯数据、格式与解析规则
├── cursormochi-app/        应用流程、设置修改/撤销策略、窄接口
├── cursormochi-platform/   文件系统与 GNOME/GIO 适配器
└── cursormochi-gtk/        GTK 界面、任务调度、依赖组装
```

允许的编译依赖：

```text
gtk      → app + platform + core
platform → app + core
app      → core
core     → 少量经过选择的纯 Rust 库
```

`core` 和 `app` 不依赖 GTK、GDK、GLib、GIO，不需要图形会话。`platform` 实现定义在 `app` 中的接口；`app` 不引用 `platform`，避免环形依赖。

| 层 | 应做 | 不应做 |
|---|---|---|
| core | 数据模型、主题解析规则、格式解码、尺寸/帧规则、限制与错误类型 | 读进程环境、写真实文件、改桌面设置、创建 GTK 对象 |
| app | 刷新/预览用例、修改计划、撤销记录、冲突检测、能力判断 | 硬编码 GSettings 实现、读取全局环境、访问 GTK 控件 |
| platform | 有界读取、搜索路径上下文、GNOME schema 探测、实际配置读写 | 决定按钮布局、在读文件时隐式改设置 |
| gtk | 展示状态、派发操作、调度任务、主线程渲染、错误提示 | 解码 Xcursor、拼 shell 命令、在信号回调里遍历整个磁盘 |

模块名可因现有仓库合理调整，但依赖方向与无副作用边界不可随意打破。

只在真实替换点定义接口，初始通常两个即可：`ThemeRepository` 和 `DesktopSettingsPort`。解析器用具体结构或函数，不为每个数据类型再定义 trait，不引入依赖注入框架。

## 3. 输入与身份模型

建议的核心概念如下；这是语义契约，不要求机械照抄字段名。

- `ThemeName`：写入桌面设置的查找名，区别于展示名称。来自路径/元数据时验证，不能含路径分隔符、NUL 或路径逃逸语义。无法表示为设置字符串的条目可展示诊断，但不可应用。
- `ThemeLocation`：来源目录、搜索根、优先顺序、文件版本信息。不要用 lossy 字符串作为文件唯一身份。
- `ThemeRecord`：同一主题查找名的全部来源目录及元数据、继承信息，不是“一个名字只保留一个目录”。
- `CursorRole`：常见角色加保留原始名称的扩展表示，不丢弃未知文件名或哈希别名。
- `ResolvedCursor`：原请求主题/角色、实际来源主题/文件、继承与别名链、解析状态、路径策略版本。
- `CursorVariant`：标称尺寸及有序动画帧。标称尺寸、实际像素尺寸、预览缩放分别存放。
- `CursorFrame`：宽高、stride、像素编码、热点、原始延迟、受控像素缓冲区。
- `Diagnostic`：严重度、错误码、操作、对象、原因与建议，不以一个 bool 代替所有结果。

建议区分结果：`Direct`、`Inherited`、`Fallback`、`Missing`、`Invalid`、`Unsupported`、`LimitExceeded`。语义不必全做 enum 顶层变体，但界面不能把它们混成“加载失败”。

源帧属于 core，`gdk::Texture` 只存在于 GTK 层。这样格式测试不用 GTK，界面不暴露解析器内部类型。

## 4. 发现目录与解析主题是两件事

### 4.1 发现：尽量不漏用户已经拥有的主题

由 platform 在启动/显式刷新时收集环境快照，生成有序搜索上下文，覆盖：

```text
XCURSOR_PATH 指定的目录（若存在）
$XDG_DATA_HOME/icons（未设置时 ~/.local/share/icons）
~/.icons
$XDG_DATA_DIRS 各目录下的 icons
系统/库配置中的其他 cursor 路径（如确实配置的 pixmaps 路径）
```

缺失目录是普通情况；拒绝无效 XDG 相对路径。环境快照传入函数，不在各模块中重复读取。XDG 默认值依据规范，而不是只写死三个目录。[S01]

### 4.2 解析：说明“这个角色究竟来自哪里”

**发现路径的并集不等于所有桌面的有效查找顺序。** Xcursor 的路径、逐文件查找和继承有自己的规则；不能直接把通用图标规范当作完整光标规范。[S02]

实现一个有明确版本、可测试的 `ResolutionPolicy`：

1. 同名主题可以在多个根目录贡献不同角色。按角色和实际 cursor 名查找，而不是先丢掉“低优先级目录”。
2. 当 `XCURSOR_PATH` 是有效显式覆盖时，解析顺序遵循已核对的库/平台语义，不把它简单追加到固定列表后。
3. 缺失角色的继承顺序、默认主题回退、别名优先顺序要有固定测试与记录。
4. 不依赖目录枚举顺序；界面按展示名称排序不应改变实际解析优先级。
5. `index.theme` 缺失不等于一定无效：存在可解析 cursor 文件的目录可以是主题；只提供有效继承信息的主题也不能直接排除。
6. 区分“指定目录资产预览”和“按主题名的有效解析预览”。默认使用后者，不让用户误以为某个被遮蔽目录必定能通过写入同名设置生效。
7. 应用设置的名字与本程序找到的具体文件来源是两层证据。无法确认宿主查找路径时标成“路径解析待验证”，不声称与所有 GTK/Qt/compositor 的结果一致。

M0 必须选定并记录受支持环境中的解析依据。可与当前 libXcursor 或其他参考加载器做开发期差分验证；参考加载器的输出不是完整桌面验证，也不意味着要让运行时依赖 Xlib。

### 4.3 链接与循环

本地已安装主题里正常的符号链接/别名要支持。不能为了安全把所有链接一律判坏，也不能无限递归跟随。

保留链接链，设置深度/条目上限和 visited 集合；断链、继承环、链接环只使受影响角色产生诊断。目录链接和实际目标必须落在已声明读取范围内；对系统 alternatives 等合法布局由 platform 明确准入，不暗中扩展成任意路径访问。

从打开后的文件句柄核对类型、大小，并有界读取。特殊文件不得造成阻塞：拒绝设备、FIFO、socket 等；必要时用审查过的安全 OS API 包装。不能以一次 canonicalize 或 metadata 检查宣称完全解决同 UID 恶意进程的竞态。

**未来包导入另用更严格策略。** 允许读取现有主题别名，不等于允许 ZIP 提交任意软链接。

## 5. 真实预览流水线

```text
选中主题/角色/尺寸
  → app 创建带 request_id 的请求
  → platform 按策略解析路径并有界读取
  → core 解码实际 Xcursor 字节
  → core 选择尺寸、生成有来源信息的预览结果
  → GTK 主线程构造纹理并播放
```

不能把 `Gdk.Cursor.new_from_name("default")` 当作任意主题的预览。该 API 查询的是当前光标主题。[S03]

不要为预览修改全局 GSettings、GTK 全局光标设置或进程环境变量。静态卡片从真实文件的第一帧生成；详情至少正确播放一套多帧 Xcursor 样例，不能用宣传 GIF 代替实际数据。

GTK 可从像素数据构造 `MemoryTexture`，但必须显式匹配字节序、通道排列、stride 和 alpha 表示，不把 packed ARGB 盲目当作 RGBA。[S04] 测试透明边缘、半透明彩色像素、非方形帧与不同热点。

预览支持原始像素查看、标称尺寸选择、检查用放大以及热点标记。检查用放大不改变主题数据或 GNOME 的 cursor-size。混合 DPI/分数缩放下不能简单说“设置为 32 就一定是 32 个屏幕物理像素”。

动画保留原始帧延迟、顺序、热点和尺寸元数据；对零/异常延迟采用明确的播放保护策略，并显示诊断，不能修改源文件。用单调时钟决定应显示的帧，避免无限累积漂移。窗口不可见/最小化时暂停动画；主题列表不同时播放全部条目。

v0.1 不要求把整个真实鼠标替换成详情预览的帧动画；先在预览画布内正确展示。实际指针变化属于显式应用后的人工验收。

## 6. 有界任务，而不是把任务写成 async 就算结束

GTK 主循环不能承担扫描、二进制解码或其他长时阻塞操作。gtk-rs 官方文档提供将阻塞任务移到工作线程并把结果送回主循环的方法。[S05]

v0.1 使用 GLib 主循环和小型有界工作队列，不额外引入 Tokio。平台设置对象按所选 API 的线程约束使用；禁止为了接口整齐，强制所有 backend 或 GTK 对象都实现 Send/Sync。

建议初始限制（项目策略，可经样例与测量调整，不是 Xcursor 格式标准）：

| 项目 | 初始预算 |
|---|---:|
| 单个元数据文件 | 256 KiB |
| 单个 cursor 输入文件 | 16 MiB |
| 单文件 TOC 条目 | 4096 |
| 单帧单边像素 | 1024 |
| 单文件总帧数 | 2048 |
| 单次解码输出像素总量 | 64 MiB |
| 解码帧缓存 | 128 MiB，按字节计费 |
| 并发重任务 | 2 |
| 继承/链接最大深度 | 32 |

所有 count × stride × height / offsets / 累积帧大小用 checked arithmetic。预算在分配前检查，不能等第三方解析器分配完才验大小。文件大小限制不能单独防止重复 TOC 引起的解码放大。超限为明确状态，不能统称“主题损坏”。

界面和纹理也必须释放：像素缓存预算不代表整个应用的内存上限。只保留可见卡片及当前详情所需纹理，淘汰时释放相应引用。

每个扫描/预览请求有版本号；新选择取代旧请求，迟到结果直接丢弃。有界队列、协作取消、分步检查要同时做。取消一个 future 或超时等待，不代表一个阻塞解析线程已被终止；需要硬终止隔离时，后续改为受限 helper 进程，v0.1 不作此安全承诺。

## 7. 解析器选型边界

先评估已有维护中的 Rust Xcursor 库，例如 `xcursor` 的解析模块；它可以作为候选，不是已审计或已选定依赖。[S06]

M0/M2 检查：维护与许可证、错误处理、畸形输入行为、内存分配、字节序、尺寸组、动画序列与延迟、所有整数运算和资源控制。

正常解码保持单一实现来源。可写小型限额/头部边界检查，但不能长期保留两套互相偏离的完整 parser。若依赖无法在分配前受控，明确换方案、上游修复或做范围受控的本地实现，并用回归样例记录原因。

第一方 crate 默认禁止自写 unsafe；这不意味着传递依赖、GTK 或整个程序不含 native/unsafe 代码。第三方代码不会因为由 safe API 调用就自动安全。

## 8. 桌面能力探测

`DesktopSettingsPort` 对上层返回能力报告，而不是一个笼统的 `supported: true`。

至少包含：会话识别依据、GNOME schema 是否存在、相关 key 是否存在/类型正确、是否可读/可写、是否存在会话/沙箱限制、实际显示后端与未确认项。

先查找 schema，再创建 settings 对象；不能假定每个系统都有 GNOME schema。GIO 提供 schema 查找和可写性查询。[S07]

只有安装了 GNOME schema，不代表当前正在运行 GNOME。未识别会话、其他桌面或权限受限时降级为浏览/预览模式，不修改配置，不伪造 KDE/XFCE backend。

主题变化外部通知只更新“当前配置”区域；不能不经用户意图覆盖尚未应用的主题选择。

`cursor-size` 的类型和范围以实际 schema 为准。主题切换默认只修改主题 key，除非用户明确修改大小。不要因为内部用了 u32 就对有符号 schema 值作未经检查的转换。

## 9. 应用与撤销：受控修改，不伪装数据库事务

### 9.1 修改意图和快照

用户点击 Apply 时，由 app 建立 `ChangeIntent`，而不是让按钮直接调用 setter。

保存所有相关 key 的：有效值、用户是否显式设置、若有则保存用户值；并记录本次真正要修改的 keys。GSettings 的 user value 与 effective value 并非永远相同。[S08]

界面中的“选择主题”不写 settings。即使使用属性绑定，也不能把选择控件双向自动绑定到宿主设置。

### 9.2 应用步骤

1. 检查环境、权限、目标解析情况和本次意图。
2. 刚要写入时重新读取配置；与制定计划时的快照比较，发现冲突就停止。
3. 逐 key 计算差异，只写本次真正改变的项；明确 no-op 不生成撤销记录。
4. 在适配器内按 API 语义处理延迟写入/批处理、setter 错误与通知。可以使用 GSettings 的 delay/apply，但不能据此承诺跨键、跨进程的完整事务。
5. 通过独立、非延迟的读取路径和必要主循环周转观察结果，避免把当前对象的未提交缓冲当成后端确认。
6. 有界等待后返回明确状态，并记录操作前值、意图、实际观察值和本次写入的项。

GSettings 写入是异步的；sync 会阻塞。不要在 GTK 主线程用 sync 或阻塞轮询“等到成功”。[S09]

状态至少区分：`Prepared`、`Writing`、`Observed`、`WriteFailed`、`Conflict`、`VerificationPending/Indeterminate`。只把 Observed 解释为“配置已更新且回读一致”，不宣称持久化确认或所有应用已换光标。

### 9.3 撤销语义

v0.1 提供单级、仅本会话的“撤销上一次修改”：A → B → C 后撤销回 B。撤销成功后清空该记录；不实现无限历史、反复切换或重做。

GSettings 的 revert 只丢弃尚未提交的延迟修改，不能撤销已经应用的主题。[S10]

撤销前比较当前配置与本程序上次观察到的操作后值：用户或其他程序已经改变任一相关项时，默认停止，显示冲突，不把它偷偷改回去。用户仍可通过重新选择并显式 Apply 发出新的意图。

恢复用户原本未显式设置的 key 时使用 reset 语义，而不是把当时有效的默认值写成永久覆盖。只恢复上次操作拥有的修改项；未动过的大小不应被重写。

系统没有为本应用提供覆盖其他程序的全局锁或通用 CAS 保证。这里是乐观检测，不是消除所有外部竞争；竞态仍可能发生，必须回读并如实报告。

### 9.4 部分失败与退出

出现部分写入时，只对确认仍保持本次写入值的、由本次操作修改的项尝试补偿。无法确认则停止并显示具体状态，不以“回滚成功”掩盖失败。补偿动作也要验证；原始错误和补偿错误同时保留。

不要在 Drop、崩溃处理或重新启动时自动恢复宿主设置。关闭窗口时若仍有写操作，显示明确状态，不在已开始写入后宣称“取消=从未修改”。

重启后撤销记录不存在，UI 应明确无可撤销项。要增加崩溃后恢复，必须另立里程碑设计持久化记录和用户确认；本版不承诺。

## 10. UI 状态与诊断

窗口可以是“主题列表 + 详情 + 当前配置 + Apply/Undo”。使用 GTK 原生可访问控件，支持键盘导航、明暗主题和清楚的错误状态；不先做品牌特效。

一个简洁的 `AppState + Action + Result` 结构即可。状态包含 selection、当前配置、能力、请求版本、忙碌状态、撤销记录和诊断。无需增加状态管理框架。

对用户显示的成功文案建议：“配置已更新，请检查实际鼠标显示。”未做视觉验证时不能显示“所有应用已生效”。

提供只读“复制诊断”入口：应用/依赖版本、会话与显示后端、搜索路径策略、配置快照、解析来源、错误码。默认脱敏 HOME 和个人目录；无遥测，不自动上传。

## 11. 测试与兼容性基线

1. core/app：无显示服务、无真实 settings 的单元与性质/畸形输入测试。
2. platform：临时目录 + 假 backend；GIO 集成使用注入的内存 backend 与私有测试 schema，不能触碰用户 dconf。[S11]
3. GTK：只读 fixture 模式进行窗口/选择/预览测试，不读取真实主题、不写真实桌面；这验证 GUI，不验证宿主光标应用。
4. 原生人工验收：真实 GNOME 会话的 Apply/Undo、动画、显示缩放、GTK/Qt/浏览器等代表性应用；逐项记测试证据。

不把 `GSETTINGS_BACKEND=memory` 环境变量作为唯一安全措施；应用测试使用明确注入。相关环境变量只在启动隔离子进程前设置，避免并行测试中修改进程全局环境。

兼容报告把以下列分开：目标 / 编译通过 / 隔离 GUI 通过 / 真实显示后端验证 / 宿主设置行为验证。Ubuntu 26.04 / GNOME 50 / Wayland 目前只是目标。

M0 依据真实环境选择 Rust 工具链、gtk-rs 版本和最低 GTK/GLib runtime。只开启用到的 API feature，不因为主机是 GNOME 50 就直接开启全套 gnome_50，也不机械用 --all-features 代表最低兼容性。[S12]

应用仓库提交 Cargo.lock，记录 MSRV 与 runtime 下限，并用 --locked 检查。锁定 Cargo 依赖并不锁定发行版系统库，所以还要记录系统依赖版本。[S13]

## 12. 为未来留边界，不为未来写空代码

- CUR/ANI：后续输入适配器产生同一核心光标模型，复用真实预览。
- 包安装：以后单独实现暂存、验证、受控安装及恢复策略；文件系统安装事务与 GNOME 设置写入不是同一事务。
- 社区目录：只产生待下载来源，下载后进入同一验证/安装流程。索引哈希能检查对应关系，但若索引与包同被替换，不能单凭哈希证明来源真实性。
- 安全隔离：公开接收不可信包前重新评估 helper 进程、权限与资源限制；v0.1 的本地预览边界不能直接当成商店安全边界。

本版不创建这些尚未实现的 crate。只有当第二个实际调用者或明确的测试边界出现，才继续拆模块。

## 13. 架构冻结条件

通过首个“真实主题发现 → 正确静态/动画预览 → 隔离设置流程”纵向切片后，冻结四层边界。后续只因可复现的问题、明确兼容性需要或真实新功能调整，不为更漂亮的目录树重写。

本版不规定虚构的二进制体积、启动时间或内存成绩；先建立测量，再优化。

技术依据见 `SOURCES.md`。预算、分层、验收用例与发布门槛属于本项目建议，不是上游官方保证。
