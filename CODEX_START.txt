请阅读并实施本仓库的 docs/IMPLEMENTATION_V2.md，遵循 docs/ARCHITECTURE_V2.md、docs/ACCEPTANCE_V2.md 和适用的 AGENTS.md。

这是 CursorMochi v0.1 的代码实施任务，不是只写计划。保持 Rust + GTK4/GIO；以稳定、兼容、正确性为优先。

先读取工作区、现有代码、git 状态和环境。若任务包是刚放入已有项目的，先合并核对规则和已有实现，不直接覆盖文件、不推倒重写。简要说明实施顺序与实际环境限制，然后从 M0 开始，依次推进到 M6；每阶段通过相关检查后继续，不需要逐项等我确认。

重点约束：
1. core/app 无 GUI/native 图形依赖，platform 实现窄接口，GTK 只展示和调度。
2. 先做真实 Xcursor 预览；同名多目录、继承、别名、动画、热点和资源限制必须有样例。
3. 预览不得改变桌面；Apply/Undo 必须有快照、差量写入、外部冲突检测和错误报告。
4. 普通测试只能使用临时目录与注入的 fake/memory settings；不得修改我当前桌面。真实 GNOME 应用/撤销留作我明确发起的验收。
5. 不增加 CUR/ANI、ZIP、安装/卸载、商店、网络、账号、KDE/XFCE 写入或打包平台。
6. 没有执行的测试不准写通过；无 GUI 时继续实现并运行 core/app 与隔离后端测试，另列真实图形待验项。
7. 新架构以文件定义的行为契约为准；若当前仓库已有更简单且等效的实现，保留并说明，不为了凑目录结构重写。

每阶段维护实际进展和验证记录，最终交付可构建代码、测试、README、诊断能力与 docs/VALIDATION.md。未解决问题如实列出，不自动开始 v0.2，也不自行推送或发行。
